<div class="flex h-screen">
  <!-- Barra de navegación lateral (Sidebar) -->
  <aside id="sidebar"
    class="bg-gray-800 text-white w-64 min-h-screen p-4 flex-col justify-between hidden md:flex transition-all duration-300">
    <div>
      <a href="#" class="flex items-center space-x-2 text-2xl font-bold mb-10 text-orange-500">
        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none"
          stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"
          class="h-8 w-8">
          <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
        </svg>
        <span>UTP+profesor</span>
      </a>
      <nav>
        <ul class="space-y-2">
          <li>
            <a href="/profesor/dashboard" (click)="navigateTo('/profesor/dashboard')"
              class="nav-link flex items-center space-x-3 px-4 py-3 hover:bg-gray-700 rounded-lg transition-colors">
              <i data-lucide="layout-grid" class="sidebar-icon"></i>
              <span>Dashboard</span>
            </a>
          </li>
          <li>
            <a href="/profesor/calendario" (click)="navigateTo('/profesor/calendario')"
              class="nav-link flex items-center space-x-3 px-4 py-3 hover:bg-gray-700 rounded-lg transition-colors">
              <i data-lucide="calendar" class="sidebar-icon"></i>
              <span>Calendario</span>
            </a>
          </li>
          <li>
            <a (click)="switchView('cursos')"
              class="nav-link flex items-center space-x-3 px-4 py-3 hover:bg-gray-700 rounded-lg transition-colors">
              <i data-lucide="book-open" class="sidebar-icon"></i>
              <span>Mis Cursos</span>
            </a>
          </li>
          <li>
            <a (click)="switchView('chat')"
              class="nav-link flex items-center space-x-3 px-4 py-3 bg-orange-500 rounded-lg text-white font-semibold">
              <i data-lucide="message-square" class="sidebar-icon"></i>
              <span>Chat</span>
            </a>
          </li>
          <li>
            <a (click)="switchView('configuracion')"
              class="nav-link flex items-center space-x-3 px-4 py-3 hover:bg-gray-700 rounded-lg transition-colors">
              <i data-lucide="settings" class="sidebar-icon"></i>
              <span>Configuración</span>
            </a>
          </li>
        </ul>
      </nav>
    </div>
    <div>
      <nav>
        <ul class="space-y-2 border-t border-gray-700 pt-4 mt-4">
          <li>
            <a href="/logout" (click)="logout()" 
              class="flex items-center space-x-3 px-4 py-3 hover:bg-gray-700 rounded-lg transition-colors">
              <i data-lucide="log-out" class="sidebar-icon"></i>
              <span>Cerrar Sesión</span>
            </a>
          </li>
        </ul>
      </nav>
    </div>
  </aside>

  <!-- Contenido principal -->
  <div class="flex-1 flex flex-col overflow-hidden">
    <!-- Cabecera (Header) -->
    <header class="bg-white shadow-md p-4 flex justify-between items-center z-10">
      <button id="menu-button" class="md:hidden" (click)="toggleSidebar()">
        <i data-lucide="menu" class="h-6 w-6"></i>
      </button>
      <div class="text-xl font-semibold md:hidden text-orange-500">UTP+profesor</div>
      <div class="flex items-center space-x-4 ml-auto">
        <button class="relative">
          <i data-lucide="bell" class="h-6 w-6 text-gray-600"></i>
          <span class="absolute -top-1 -right-1 h-3 w-3 bg-red-500 rounded-full border-2 border-white"></span>
        </button>
        <div class="flex items-center space-x-2">
          <img src="https://placehold.co/40x40/EFEFEF/333333?text={{ getUserInitials() }}" 
               alt="Avatar de profesor" class="w-10 h-10 rounded-full">
          <div class="text-right hidden sm:block">
            <p class="font-semibold text-sm">{{ getUserName() }}</p>
            <p class="text-xs text-gray-500">Profesor</p>
          </div>
          <i data-lucide="chevron-down" class="h-5 w-5 text-gray-500"></i>
        </div>
      </div>
    </header>

    <!-- Área de contenido -->
    <main class="flex-1 overflow-y-auto bg-slate-100 p-4 sm:p-6 lg:p-8">
      <!-- Vista Chat (Visible por defecto) -->
      <section id="chat-content" class="content-view">
        <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center mb-6">
          <h1 class="text-3xl font-bold text-gray-800">Chat</h1>
          <div class="flex items-center space-x-2 mt-4 sm:mt-0">
            <div class="relative">
              <input type="text" placeholder="Buscar conversaciones..." 
                     [(ngModel)]="searchTerm"
                     class="pl-10 pr-4 py-2 border rounded-lg w-full sm:w-auto">
              <i data-lucide="search" class="absolute left-3 top-1/2 -translate-y-1/2 w-5 h-5 text-gray-400"></i>
            </div>
            <button (click)="createNewChat()" 
                    class="bg-orange-500 text-white font-bold py-2 px-4 rounded-lg hover:bg-orange-600 transition-colors flex items-center space-x-2">
              <i data-lucide="plus" class="w-5 h-5"></i>
              <span>Nuevo Chat</span>
            </button>
          </div>
        </div>

        <div class="flex h-[calc(100vh-200px)] gap-6">
          <!-- Panel de conversaciones -->
          <div class="w-full sm:w-1/3 bg-white rounded-lg shadow-sm overflow-hidden">
            <div class="p-4 border-b border-gray-200">
              <h2 class="font-semibold text-gray-800">Conversaciones</h2>
            </div>
            
            <!-- Filtros de conversaciones -->
            <div class="p-4 border-b border-gray-200">
              <div class="flex space-x-2">
                <button (click)="filterChats('all')" 
                        [class]="activeChatFilter === 'all' ? 'bg-orange-500 text-white' : 'bg-gray-100 text-gray-600 hover:bg-gray-200'"
                        class="px-3 py-1 text-sm rounded-full transition-colors">
                  Todas
                </button>
                <button (click)="filterChats('students')" 
                        [class]="activeChatFilter === 'students' ? 'bg-orange-500 text-white' : 'bg-gray-100 text-gray-600 hover:bg-gray-200'"
                        class="px-3 py-1 text-sm rounded-full transition-colors">
                  Estudiantes
                </button>
                <button (click)="filterChats('teachers')" 
                        [class]="activeChatFilter === 'teachers' ? 'bg-orange-500 text-white' : 'bg-gray-100 text-gray-600 hover:bg-gray-200'"
                        class="px-3 py-1 text-sm rounded-full transition-colors">
                  Colegas
                </button>
                <button (click)="filterChats('groups')" 
                        [class]="activeChatFilter === 'groups' ? 'bg-orange-500 text-white' : 'bg-gray-100 text-gray-600 hover:bg-gray-200'"
                        class="px-3 py-1 text-sm rounded-full transition-colors">
                  Grupos
                </button>
              </div>
            </div>

            <!-- Lista de conversaciones -->
            <div class="overflow-y-auto h-full">
              <div *ngFor="let conversation of filteredConversations; trackBy: trackByConversationId" 
                   (click)="selectConversation(conversation)"
                   [class]="selectedConversation?.id === conversation.id ? 'bg-orange-50 border-r-4 border-orange-500' : 'hover:bg-gray-50'"
                   class="p-4 border-b border-gray-100 cursor-pointer transition-colors">
                <div class="flex items-start space-x-3">
                  <div class="relative">
                    <img [src]="getConversationAvatar(conversation)" 
                         [alt]="conversation.name" 
                         class="w-12 h-12 rounded-full">
                    <div *ngIf="conversation.online" 
                         class="absolute -bottom-1 -right-1 w-4 h-4 bg-green-500 border-2 border-white rounded-full"></div>
                  </div>
                  <div class="flex-1 min-w-0">
                    <div class="flex items-center justify-between">
                      <h3 class="font-semibold text-gray-900 truncate">{{ conversation.name }}</h3>
                      <span class="text-xs text-gray-500">
                        {{ formatTime(conversation.lastMessageTime) }}
                      </span>
                    </div>
                    <p class="text-sm text-gray-600 truncate">
                      {{ conversation.lastMessage }}
                    </p>
                    <div class="flex items-center justify-between mt-1">
                      <span class="text-xs text-gray-500">{{ getConversationType(conversation) }}</span>
                      <div *ngIf="conversation.unreadCount > 0" 
                           class="bg-orange-500 text-white text-xs rounded-full px-2 py-1 min-w-[20px] text-center">
                        {{ conversation.unreadCount }}
                      </div>
                    </div>
                  </div>
                </div>
              </div>

              <!-- Mensaje cuando no hay conversaciones -->
              <div *ngIf="filteredConversations.length === 0" class="p-8 text-center">
                <i data-lucide="message-square" class="h-12 w-12 text-gray-300 mx-auto mb-4"></i>
                <h3 class="text-lg font-semibold text-gray-600 mb-2">No hay conversaciones</h3>
                <p class="text-gray-500">Selecciona "Nuevo Chat" para comenzar una conversación</p>
              </div>
            </div>
          </div>

          <!-- Panel de mensajes -->
          <div class="flex-1 bg-white rounded-lg shadow-sm flex flex-col">
            <!-- Header del chat -->
            <div *ngIf="selectedConversation" class="p-4 border-b border-gray-200">
              <div class="flex items-center justify-between">
                <div class="flex items-center space-x-3">
                  <img [src]="getConversationAvatar(selectedConversation)" 
                       [alt]="selectedConversation.name" 
                       class="w-10 h-10 rounded-full">
                  <div>
                    <h2 class="font-semibold text-gray-800">{{ selectedConversation.name }}</h2>
                    <p class="text-sm text-gray-500">{{ getConversationType(selectedConversation) }}</p>
                  </div>
                </div>
                <div class="flex space-x-2">
                  <button (click)="searchInChat()" 
                          class="p-2 text-gray-600 hover:bg-gray-100 rounded-full">
                    <i data-lucide="search" class="w-5 h-5"></i>
                  </button>
                  <button (click)="showChatInfo()" 
                          class="p-2 text-gray-600 hover:bg-gray-100 rounded-full">
                    <i data-lucide="info" class="w-5 h-5"></i>
                  </button>
                  <button (click)="startVideoCall()" 
                          class="p-2 text-gray-600 hover:bg-gray-100 rounded-full">
                    <i data-lucide="video" class="w-5 h-5"></i>
                  </button>
                  <button (click)="startVoiceCall()" 
                          class="p-2 text-gray-600 hover:bg-gray-100 rounded-full">
                    <i data-lucide="phone" class="w-5 h-5"></i>
                  </button>
                </div>
              </div>
            </div>

            <!-- Mensajes -->
            <div class="flex-1 overflow-y-auto p-4 space-y-4" id="messages-container">
              <div *ngIf="!selectedConversation" class="h-full flex items-center justify-center">
                <div class="text-center">
                  <i data-lucide="message-circle" class="h-16 w-16 text-gray-300 mx-auto mb-4"></i>
                  <h3 class="text-lg font-semibold text-gray-600 mb-2">Selecciona una conversación</h3>
                  <p class="text-gray-500">Elige una conversación de la lista para comenzar a chatear</p>
                </div>
              </div>

              <div *ngIf="selectedConversation">
                <div *ngFor="let message of currentMessages; trackBy: trackByMessageId" 
                     [class]="message.senderId === user?.id ? 'text-right' : 'text-left'"
                     class="mb-4">
                  <div [class]="message.senderId === user?.id ? 'ml-auto max-w-xs lg:max-w-md' : 'mr-auto max-w-xs lg:max-w-md'">
                    <div [class]="message.senderId === user?.id 
                                  ? 'bg-orange-500 text-white rounded-l-lg rounded-tr-lg' 
                                  : 'bg-gray-200 text-gray-800 rounded-r-lg rounded-tl-lg'"
                         class="px-4 py-2">
                      <p class="text-sm">{{ message.content }}</p>
                    </div>
                    <p class="text-xs text-gray-500 mt-1">
                      {{ formatTime(message.timestamp) }}
                      <span *ngIf="message.senderId === user?.id && message.read" class="ml-1">
                        <i data-lucide="check-check" class="w-3 h-3 inline"></i>
                      </span>
                    </p>
                  </div>
                </div>
              </div>
            </div>

            <!-- Input de mensaje -->
            <div *ngIf="selectedConversation" class="p-4 border-t border-gray-200">
              <div class="flex items-end space-x-2">
                <button (click)="toggleEmojiPicker()" 
                        class="p-2 text-gray-600 hover:bg-gray-100 rounded-full">
                  <i data-lucide="smile" class="w-5 h-5"></i>
                </button>
                
                <div class="flex-1 relative">
                  <textarea [(ngModel)]="newMessage" 
                            (keydown.enter)="sendMessage($event)"
                            (keydown.ctrl.enter)="newMessage += '\n'"
                            placeholder="Escribe un mensaje..."
                            class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-orange-500 focus:border-orange-500 resize-none"
                            rows="1"></textarea>
                </div>
                
                <button (click)="sendMessage()" 
                        [disabled]="!newMessage.trim()"
                        class="bg-orange-500 text-white p-2 rounded-full hover:bg-orange-600 disabled:bg-gray-300 disabled:cursor-not-allowed">
                  <i data-lucide="send" class="w-5 h-5"></i>
                </button>
              </div>

              <!-- Indicador de escritura -->
              <div *ngIf="isTyping" class="text-xs text-gray-500 mt-1">
                {{ selectedConversation.name }} está escribiendo...
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- Vista Cursos (Oculta por defecto) -->
      <section id="cursos-content" class="content-view hidden">
        <div class="flex justify-between items-center mb-6">
          <h1 class="text-3xl font-bold text-gray-800">Mis Cursos</h1>
          <button (click)="navigateTo('/profesor/gestion-curso')"
            class="bg-orange-500 text-white font-bold py-2 px-4 rounded-lg hover:bg-orange-600 transition-colors">
            Gestionar Cursos
          </button>
        </div>

        <!-- Lista de Cursos -->
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
          <div *ngFor="let curso of misCursos" 
               class="bg-white rounded-lg shadow-sm overflow-hidden hover:shadow-md transition-shadow cursor-pointer"
               (click)="navigateTo('/profesor/gestion-curso?id=' + curso.id)">
            <img src="https://placehold.co/600x400/FFDDC1/4A4A4A?text={{ curso.nombre }}" 
                 alt="Imagen del curso" class="w-full h-40 object-cover">
            <div class="p-4">
              <h3 class="font-bold text-lg mb-2">{{ curso.nombre }}</h3>
              <p class="text-sm text-gray-600 mb-2">{{ curso.codigo }} - {{ curso.descripcion }}</p>
              <div class="flex items-center justify-between text-sm text-gray-500">
                <span>{{ getTotalEstudiantes(curso.id) }} estudiantes</span>
                <span class="text-orange-600 font-medium">Ver detalles</span>
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- Vista Configuración (Oculta por defecto) -->
      <section id="configuracion-content" class="content-view hidden">
        <h1 class="text-3xl font-bold text-gray-800 mb-6">Configuración</h1>
        <div class="bg-white rounded-lg shadow-sm p-6">
          <p class="text-gray-600">Panel de configuración del chat.</p>
        </div>
      </section>
    </main>
  </div>
</div>

<!-- Modal de nueva conversación -->
<div *ngIf="showNewChatModal" class="fixed inset-0 bg-gray-600 bg-opacity-50 flex items-center justify-center z-50">
  <div class="bg-white rounded-lg shadow-xl w-11/12 md:w-1/2 lg:w-1/3">
    <div class="p-6">
      <div class="flex justify-between items-center mb-4">
        <h3 class="text-lg font-semibold text-gray-900">Nueva Conversación</h3>
        <button (click)="closeNewChatModal()" class="text-gray-400 hover:text-gray-600">
          <i data-lucide="x" class="w-6 h-6"></i>
        </button>
      </div>
      
      <div class="space-y-4">
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-2">Buscar usuarios</label>
          <input type="text" [(ngModel)]="userSearchTerm" 
                 placeholder="Escribe el nombre o email..."
                 class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-orange-500 focus:border-orange-500">
        </div>
        
        <div class="max-h-40 overflow-y-auto">
          <div *ngFor="let user of filteredUsers" 
               (click)="selectUserForChat(user)"
               class="flex items-center space-x-3 p-2 hover:bg-gray-50 cursor-pointer rounded">
            <img [src]="user.avatar || 'https://placehold.co/40x40/EFEFEF/333333?text=' + user.name[0]" 
                 [alt]="user.name" class="w-10 h-10 rounded-full">
            <div>
              <p class="font-medium text-gray-900">{{ user.name }}</p>
              <p class="text-sm text-gray-500">{{ user.role }} - {{ user.email }}</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- Script para funcionalidad de lucide icons -->
<script>
  document.addEventListener('DOMContentLoaded', () => {
    if (typeof lucide !== 'undefined') {
      lucide.createIcons();
    }
  });
</script>